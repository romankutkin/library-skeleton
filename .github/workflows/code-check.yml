name: Code Check
on:
    push:
        branches: [ develop ]
    pull_request:
        branches: [ develop ]

jobs:
    code-check:
        runs-on: ubuntu-latest

        steps:
            -
                name: Checkout code
                uses: actions/checkout@v4
